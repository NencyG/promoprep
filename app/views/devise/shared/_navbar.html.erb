<div class="navbar-div">
  <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <%= link_to "Promopre", root_path, class: "link-name"%>
      <% if current_user.present? %>
        <%= link_to "Company", companies_path, class: "link-name"%>
        <%= link_to "Promo", promos_path, class: "link-name"%>
      <% end %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
          </li>
        </ul>
        <% if current_user.present? %>
          <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "link-name"%>
        <% else %>
          <a href="/users/sign_in" class="link-name">Login</a>
        <% end %>
      </div>
    </div>
  </nav>
</div>
