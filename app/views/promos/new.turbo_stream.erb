<%= turbo_stream.update("filter-list") do %>
    <%= select_tag 'promo[filter_option_ids]', options_from_collection_for_select(@select_company, :id, :name), { include_blank: 'Select City' } %>
<% end %>